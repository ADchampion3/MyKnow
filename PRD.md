# 个人知识库与 AI Agent

## 1. 文档状态

- 状态：已确认的产品定义
- 版本：0.1
- 日期：2026-08-18
- 首要用户：个人研究与学习者
- 首版形态：本地优先的单用户 Web 应用

## 2. 产品概述

这是一个面向个人研究与学习的知识库系统。用户可以导入和更新资料，将资料组织到可复用的知识库/空间中，在 RAG 对话时明确选择资料范围，并让 AI Agent 将杂乱资料整理为可审阅、可追溯、可回滚的 Wiki。

系统的核心原则是：原始资料不可变，Agent 产物必须引用来源，任何写入都先形成可审核的变更计划。

## 3. 产品目标

### 3.1 首版目标

1. 让用户从 Markdown、纯文本、PDF 和网页导入资料。
2. 将资料解析、分块、索引，并保留来源和版本关系。
3. 让用户创建知识库/空间、维护标签，并在对话中勾选检索范围。
4. 让 Agent 提议生成或更新 Wiki、摘要、标签、资料链接，并发现重复与冲突。
5. 让用户以章节、段落或事实条目为单位审核变更，查看 diff、接受、拒绝或回滚。
6. 提供严格库内问答和开放问答两种模式，关键结论必须带引用。

### 3.2 非目标

首版不包含：

- 多用户协作、实时编辑和复杂权限
- 原生移动端
- 多 Agent 协作
- 自动删除或覆盖原始资料
- 自动网页定期同步
- 知识图谱可视化
- 手写 OCR、签名/印章识别和超出 Sprint 2 能力边界的版面理解
- 自训练嵌入模型或排序模型

## 4. 核心用户流程

### 4.1 资料到 Wiki

1. 用户导入 Markdown/TXT/PDF，或输入 URL。
2. 系统保存原始文件或网页快照，并记录来源、时间和内容指纹。
3. 后台任务解析内容、提取结构、分块并建立全文/向量索引。
4. 用户将资料放入一个或多个知识库，并设置标签和敏感度策略。
5. Agent 生成 Wiki 整理变更计划，包括章节、事实、引用、标签和链接建议。
6. 用户逐项或批量审核变更。
7. 系统写入 Wiki 派生层，保留版本、diff 和原始来源引用。

### 4.2 RAG 问答

1. 用户在对话中选择一个或多个知识库。
2. 系统展示预计使用的资料范围，用户可调整。
3. 系统执行关键词 + 向量混合检索和重排序。
4. 模型生成答案，关键结论引用具体文档、版本和位置。
5. 若严格库内模式找不到证据，系统明确表示不知道。
6. 开放问答模式可补充外部知识，但必须与库内证据清楚区分。

### 4.3 资料更新

1. 用户重新上传资料或更新网页快照。
2. 系统保留旧版本并进行增量解析、分块和索引。
3. 系统标记受影响的 Wiki 章节、事实和引用。
4. Agent 提议更新受影响内容，用户审核后写入新版本。

## 5. 功能需求

### 5.1 资料管理

- 支持 Markdown、TXT、native/OCR PDF 和 URL 导入；PDF 导入显式选择 OCR 模式与 local/cloud/paddleocr Provider。
- URL 导入保存正文快照、原始 URL、抓取时间和标题。
- 原始资料不可变；更新产生新版本。
- 资料可归属多个知识库，不通过复制实现复用。
- 支持标签、来源、创建时间、更新时间和敏感度策略。
- 展示导入、解析、索引任务状态、进度、重试和错误日志。

### 5.2 知识库与 Wiki

- 用户可创建知识库和子空间。
- Wiki 以 Markdown/文档为主要存储形式，提供主题树/目录浏览。
- 默认模板包含：定义、核心观点、证据、争议、相关资料、待验证问题。
- 知识库可以配置自己的 Wiki 模板。
- Wiki 是原始资料的可追溯派生层，不是原文替代品。
- 支持章节、段落或事实条目的版本、diff 和单次变更恢复。

### 5.3 Agent

- 生成和更新 Wiki 页面。
- 提取摘要、关键概念和标签。
- 建立资料与 Wiki 之间的链接。
- 发现重复资料、重复观点和相互冲突的事实。
- 对每个建议提供依据和来源。
- 将写入操作组织为可审核的变更计划。
- 低风险标签等变更支持批量审核；事实改写和冲突合并必须明确确认。
- 禁止删除或覆盖原始资料。

### 5.4 RAG 与问答

- 对话支持手动勾选知识库/空间。
- 发送前显示实际检索范围。
- 支持严格库内和开放问答模式。
- 使用关键词 + 向量的混合检索，并可加入简单重排序。
- 答案显示使用的知识库、文档、版本和原文定位。
- 无足够证据时必须明确说明未知，不得伪造引用。
- 记录检索到的 chunk、重排序结果和模型调用日志，便于调试。

### 5.5 模型与隐私

- 通过统一 Provider 接口接入 OpenAI-compatible API。
- 至少区分嵌入、问答和 Agent 整理模型。
- 支持按知识库设置“允许云端/仅本地/禁止 AI”的默认策略。
- 单份资料可收紧知识库策略；实际调用采用最严格限制。
- 首版保留 Ollama 等本地模型接口，但本地模型不应阻塞核心流程。
- API 密钥不得写入前端或明文保存于业务数据中。

## 6. 交互与信息架构

首版工作台采用三栏结构：

- 左侧：知识库树、空间和检索范围
- 中间：资料、Wiki、版本和审核 diff
- 右侧：Agent 对话、变更计划和任务状态

主导航应优先支持：知识库浏览、待审核变更、资料导入/更新、检索问答和任务日志。

## 7. 技术方向

- 前端：Next.js + React + TypeScript
- 后端：NestJS + TypeScript
- 数据库：Drizzle ORM + SQLite
- 原始文件：本地文件系统
- 向量索引：本地 VectorStore 实现（如 LanceDB 或 SQLite 向量扩展）
- Agent 编排：LangGraph.js，应用自身掌控领域状态和权限
- 部署：模块化单体；Node API 与后台 worker 可作为独立进程运行
- 设计要求：主数据库保存资料、版本、chunk、引用、任务和审核状态；向量索引仅作为检索加速层

## 8. 可观察性

系统至少记录并在界面中展示：

- 导入、解析、索引、Agent 任务的状态、进度、重试和错误
- Agent 的计划、步骤和工具调用
- 模型耗时、token、成本和失败原因
- 检索到的 chunk、来源和重排序信息
- 每次 Wiki 变更的操作者、时间、依据和版本

## 9. 验收标准

首版在以下条件下视为达到可用标准：

- 100 份混合资料可以稳定导入、更新和检索。
- 80% 以上问答关键结论具有正确引用。
- 70% 以上 Agent 整理建议可以直接采纳。
- 资料更新后，受影响 Wiki 内容能够被标记。
- 单份资料从导入到可问答的时间不超过 2 分钟（不含超长外部模型排队）。
- 建立 30～50 个真实问题的回归测试集。
- 测试集包含重复、冲突、更新和无答案样本。
- 用户连续两周使用系统完成至少一次真实研究任务，并愿意继续使用。

## 10. 后续路线图候选

以下内容不属于 MVP，但可作为后续迭代：

- 本地目录监视和网页定期同步
- 手写 OCR、更高精度表格/公式识别和复杂 PDF 布局树
- 知识图谱和关系可视化
- 学习卡片、研究报告和学习路径
- 多用户、团队空间和细粒度权限
- 原生桌面/移动客户端
- 持久化任务队列和更复杂的模型评估体系
